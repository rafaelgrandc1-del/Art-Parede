package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Luxury Color Palette for Art Parede
val LuxuryGold = Color(0xFFD4AF37) // Metallic Gold Accent
val DarkGold = Color(0xFFA68026) // Deep Gold for borders/edges
val Champagne = Color(0xFFF3E5AB) // Soft luxurious cream accent

// Dark Theme Colors
val CharcoalBg = Color(0xFF151413) // Deep, warm, near-black slate
val SurfaceCharcoal = Color(0xFF1F1D1C) // Slightly lighter slate for cards
val SurfaceCharcoalElevated = Color(0xFF2B2826) // Elevated card background
val TextLinen = Color(0xFFEBE6DD) // Warm off-white for primary text
val TextOatmeal = Color(0xFFAFA99E) // Muted warm-gray for secondary text

// Light Theme Colors
val SandBg = Color(0xFFF5F2ED) // Elegant, light linen background (#F5F2ED)
val SurfaceSand = Color(0xFFEBE8E3) // Slightly darker than SandBg for structured cards
val SurfaceSandElevated = Color(0xFFDFDCD7) // Richer warm card accent
val TextEspresso = Color(0xFF1A1A1A) // High-contrast near-black for bold headers (#1A1A1A)
val TextTruffle = Color(0xFF555555) // Neutral gray for secondary text
val IvoryBorder = Color(0xFFDCD9D4) // Subtle divider border

